import {BASE_URL as BASE_URL_ENV} from '@env';
import {ENV as ENV_ENV} from '@env';
import {AUTH_TOKEN as AUTH_TOKEN_ENV} from '@env';

export const BASE_URL = BASE_URL_ENV;
export const ENV = ENV_ENV;
export const AUTH_TOKEN = AUTH_TOKEN_ENV;
