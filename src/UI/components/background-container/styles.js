import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
  },
  childContainer: {
    zIndex: 2,
  },

  background: {
    width: '100%',
    height: '100%',
    position: 'absolute',
    zIndex: 1,
  },
});
